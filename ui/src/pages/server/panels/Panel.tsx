// Re-export of the shared ops-grade Panel chrome (now used by both the server
// cockpit and the admin node cockpit). Kept here so existing server-panel
// imports (`./Panel`) stay stable.
export { Panel } from '@/components/ui/Panel';
